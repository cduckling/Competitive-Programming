#include <iostream>
using namespace std;

#define io ios_base::sync_with_stdio(false); cin.tie(NULL);

int a[401];
int dp[401][401][401];

void solve() {
    int n {}, m {}; cin >> n >> m;

    for (int i {1}; i <= n; ++i) cin >> a[i];

    for (int i {1}; i <= n; ++i) {
        for (int j {1}; j <= n; ++j) {
            for (int k {1}; k <= n; ++k) {
                
            }
        }
    }
}

int main() {
    io

    solve();

    return 0;
}